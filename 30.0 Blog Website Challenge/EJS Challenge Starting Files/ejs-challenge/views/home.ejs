<%- include("partials/header"); -%>

    <h1>Home</h1>
    <p>
        <%= startingPageContent %>
    </p>

    <% blogPosts.forEach(function(post){ %>

        <h1> <%= post.title %> </h1>
        <p> <%= post.content.substring(0,100) + "..." %> <a href="/randomPosts/<%= post.title %>">Read More</a></p>

            <% }); %>


                <%- include("partials/footer"); -%>